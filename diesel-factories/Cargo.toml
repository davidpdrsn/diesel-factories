[package]
name = "diesel-factories"
version = "0.0.1"
authors = ["David Pedersen <david.pdrsn@gmail.com>"]
edition = "2018"
description = "Test factories for Diesel"
license = "MIT"

[dependencies]
diesel = { version = "1.3.3" }
diesel-factories-code-gen = { version = "0.0.1", path = "../diesel-factories-code-gen" }
lazy_static = "1.3.0"

[dev_dependencies]
diesel = { version = "1.3.3", features = ["postgres"] }
